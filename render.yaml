services:
  - type: web
    name: moodplace
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn server:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: VIVO_API_KEY
        sync: false
